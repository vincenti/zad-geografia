{% extends '::base.html.twig' %}
{% block body %}
    <table>
        <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Mezczyzni</th>
            <th>Kobiety</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.mezczyzni }}</td>
                <td>{{ entity.kobiety }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}